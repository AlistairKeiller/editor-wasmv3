name: build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - run: ninja -C build -- build